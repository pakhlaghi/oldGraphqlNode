const styles = theme => ({
  container: {
    flexGrow: "1",
    alignItems: "center",
    justifyContent: "center",
    display: "flex"
  },
  progress: {
    margin: theme.spacing.unit * 2
  }
});

export default styles;
