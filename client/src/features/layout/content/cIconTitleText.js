import React from "react";
// UI
import styles from "./cIconTitleText.style";
import { withStyles } from "@material-ui/core/styles";

const CIconTitleText = ({ classes }) => {
  return <section className={classes.footer}>cIconTitleText</section>;
};

export default withStyles(styles)(CIconTitleText);
